# Client architecture

`pages/` owns screen-level composition, `components/` owns reusable UI, `services/` owns request boundaries, `data/` contains mock fallbacks, and `constants/` owns route/label/config values.
